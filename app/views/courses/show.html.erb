<div>
	Name : <%= @course.name %><br>
	Description : <%= @course.description %><br><br>

</div>
<%= link_to "Edit Course", edit_course_path(@course) %> | 
<%= link_to "Delete Course", course_path(@course), data: { method: :delete, confirm: "Are you sure?" } %>

