<h1>Edit Book</h1>

<%= form_for @book do |form| %> 
	<div>
		<%= form.label :name %>
		<%= form.text_field :name %>
	</div><br>

	<div>
        <%#= form.hidden_field :auther_id, value:params[:format].to_i %>
	</div><br>

		<%= form.submit %>

<% end %>