<h1>Edit Student</h1>

<%= form_for @student do |form| %> 
	<div>
		<%= form.label :name %><br>
		<%= form.text_field :name %>
	</div><br>

	<div>
		<%= form.label :description %><br>
		<%= form.text_area :description %>
	</div><br>

	<%= form.submit %>

<% end %>